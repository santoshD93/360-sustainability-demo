{
  "name": "360-sustainability-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@types/node": "^24.2.0",
    "@vercel/node": "^5.3.11",
    "openai": "^5.12.0"
  }
}
